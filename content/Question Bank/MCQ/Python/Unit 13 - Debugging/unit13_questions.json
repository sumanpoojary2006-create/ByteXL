[
  {
    "set": 1,
    "difficulty": "medium",
    "bloom": "analyze",
    "subtopic": "debugging-techniques",
    "description": "A developer's code throws an error intermittently, so they wrap it in a broad try/except that silently ignores the exception and moves on, without ever finding the cause. Why does the debugging mindset discourage this?",
    "explanation": "Hiding a bug without understanding its cause does not make it go away; an unexplained bug typically resurfaces later, often in a worse or harder-to-trace form, whereas understanding the root cause prevents that recurrence.",
    "options": [
      "Broad try/except blocks cannot handle intermittent errors",
      "Silently ignoring exceptions always improves performance",
      "It does not matter, since the error message is gone",
      "The root cause remains and can trigger the failure again"
    ],
    "answer": "D"
  },
  {
    "set": 1,
    "difficulty": "medium",
    "bloom": "apply",
    "subtopic": "debugging-techniques",
    "description": "A data-import bug is being traced across two functions.\n```python\ndef load_counts(raw_counts):\n    print(f\"loaded: {raw_counts!r}\")\n    return raw_counts\n\ndef total_rsvps(raw_counts):\n    counts = load_counts(raw_counts)\n    print(f\"about to sum: {counts!r}\")\n    return sum(counts)\n\ntotal_rsvps([2, 3, \"1\"])\n```\nBoth prints show the bad \"1\" already present. What does this tell you?",
    "explanation": "Since the bad value appears unchanged at every printed step, the problem is not introduced inside `load_counts` or `total_rsvps`; it must already exist in whatever code built the original list before either function ran.",
    "options": [
      "The bug is inside `total_rsvps`, specifically in the `sum()` call",
      "There is no bug, since both prints ran successfully",
      "The return statement in `load_counts` created the bad value",
      "The bad value was created before either function ran"
    ],
    "answer": "D"
  },
  {
    "set": 1,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "assertions",
    "description": "A registration form uses an assertion for user input.\n```python\nage_text = input(\"Enter your age: \")\nassert age_text.isdigit(), \"must be digits\"\n```\nWhy is this considered a misuse of assert?",
    "explanation": "assert is meant for catching bugs in your own logic that should be logically impossible, not for validating ordinary, expected bad input from a user; using assert here crashes the whole program on normal bad input that should instead be handled gracefully with if/raise or try/except.",
    "options": [
      "assert always passes no matter what the input value is",
      "assert cannot be used to check string methods like `isdigit()`",
      "Expected bad input needs explicit validation, not `assert`",
      "Assertions are automatically removed during every Python run"
    ],
    "answer": "C"
  },
  {
    "set": 1,
    "difficulty": "easy",
    "bloom": "understand",
    "subtopic": "assertions",
    "description": "A developer is reviewing a teammate's code that relies on assert statements to catch logic errors during testing. What happens when an assert statement's condition evaluates to `False`?",
    "explanation": "If an assert's condition is false, Python immediately raises an `AssertionError` at that exact line, optionally with the message provided after the comma; if the condition is true, nothing happens and execution simply continues.",
    "options": [
      "The condition is ignored and execution continues",
      "Python raises `AssertionError` at that line",
      "Python prints a warning but continues running normally",
      "The program pauses and waits for user input"
    ],
    "answer": "B"
  },
  {
    "set": 1,
    "difficulty": "easy",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "A teammate is stuck mid-bugfix on a chat app: a message queue sometimes drops the last entry, and she wants to pause the script at the exact line where messages are appended so she can inspect variables interactively. Which function, when called inside your code, pauses execution at that exact line and drops you into an interactive (Pdb) prompt?",
    "explanation": "Python's built-in `breakpoint()` function, called anywhere in your code, pauses execution at that exact line and hands control to an interactive debugger prompt, letting you inspect any variable available at that point.",
    "options": [
      "`debug()` function",
      "`pause()` function",
      "`breakpoint()`",
      "`stop()` function"
    ],
    "answer": "C"
  },
  {
    "set": 1,
    "difficulty": "medium",
    "bloom": "apply",
    "subtopic": "debugging-techniques",
    "description": "A debugger is paused just before an accumulator update.\n```\n> tracker.py(4)total_rsvps()\n-> total += attendee.rsvp_count\n(Pdb) total\n0\n(Pdb) attendee.rsvp_count\n2\n(Pdb) n\n> tracker.py(3)total_rsvps()\n-> for attendee in attendees:\n(Pdb) total\n```\nWhat value does pdb report for total on the second check?",
    "explanation": "Typing n ran the line total += `attendee.rsvp_count`, which added the current attendee's `rsvp_count` of 2 to the previous total of 0, so the second check correctly reports total as 2.",
    "options": [
      "0, unchanged since nothing happened",
      "An error, because total was never defined",
      "2, because the addition line ran",
      "`None`, because pdb reports only object references"
    ],
    "answer": "C"
  },
  {
    "set": 1,
    "difficulty": "easy",
    "bloom": "understand",
    "subtopic": "logging",
    "description": "A developer setting up logging for a new fitness-tracking app needs to choose the right severity level for each message, from routine step-count updates to a failed database write. What are Python's five standard logging levels, listed from least to most severe?",
    "explanation": "The logging module defines five standard levels in increasing order of severity: DEBUG, INFO, WARNING, ERROR, and CRITICAL, and choosing the right level for each message is the entire point of using logging instead of plain print statements.",
    "options": [
      "ERROR, CRITICAL, WARNING, INFO, DEBUG",
      "CRITICAL, ERROR, WARNING, INFO, DEBUG",
      "INFO, DEBUG, ERROR, WARNING, CRITICAL",
      "DEBUG, INFO, WARNING, ERROR, CRITICAL"
    ],
    "answer": "D"
  },
  {
    "set": 1,
    "difficulty": "medium",
    "bloom": "analyze",
    "subtopic": "logging",
    "description": "A program shipped to production has `basicConfig(level=logging.DEBUG)` still set, flooding the real log file with fine-grained debug detail meant only for development. What is the simplest fix, and why does logging make this easy?",
    "explanation": "Changing the single level setting to something like WARNING or INFO silences the debug and possibly info messages without touching any of the individual `logging.debug()` or `logging.info()` calls scattered through the code, which is exactly the selective quieting plain `print()` statements cannot offer.",
    "options": [
      "Logging cannot be filtered once a program is running",
      "Raise the configured level; keep the existing log calls",
      "Every `logging.debug()` call must be deleted by hand",
      "The only fix is to replace all logging calls with `print()`"
    ],
    "answer": "B"
  },
  {
    "set": 1,
    "difficulty": "easy",
    "bloom": "understand",
    "subtopic": "debugging-workflow",
    "description": "A developer is debugging a large, multi-file program and wants to isolate the bug efficiently rather than searching everywhere at once. What is the first recommended step when isolating a minimal failing case?",
    "explanation": "The first move is confirming the bug does not actually require the full program, like file reading or command-line arguments, by reproducing it outside the full program using only the specific data and function involved.",
    "options": [
      "Reproduce the same bug with only essential code and data",
      "Delete the entire project completely and start over from scratch",
      "Immediately ask an AI assistant without trying anything first",
      "Add print statements to every function in every file"
    ],
    "answer": "A"
  },
  {
    "set": 1,
    "difficulty": "hard",
    "bloom": "analyze",
    "subtopic": "debugging-workflow",
    "description": "An AI assistant suggests a fix for a bug. According to responsible AI-assisted debugging, what must happen before trusting and using that fix?",
    "explanation": "The fix deserves the same scrutiny as code you wrote yourself: read and understand it to confirm it actually addresses the diagnosed cause, then rerun your own minimal failing case to verify the original error is genuinely gone, since a plausible-looking fix can still address the wrong problem.",
    "options": [
      "Understand it, then rerun the original failing case",
      "Paste it immediately because generated fixes are already verified",
      "Reject all AI suggestions automatically without ever reading them at all",
      "Ask a second AI assistant to verify the first one's answer instead of testing it yourself"
    ],
    "answer": "A"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "What is the correct mindset to hold when a program crashes with a traceback?",
    "explanation": "A crash is information about a specific gap between what you expected and what actually happened, not a judgment on your skill as a programmer; treating it that way keeps you focused on the gap instead of on self-doubt.",
    "options": [
      "It means you are simply not cut out for programming",
      "It means the Python interpreter has been permanently damaged",
      "It should be ignored and the program rerun until it works",
      "Treat it as useful evidence about an expectation mismatch"
    ],
    "answer": "D"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "A device's grading script throws a traceback moments before a submission deadline, and panic sets in as red error text fills the terminal. A senior helps calmly, and the device wonders whether they could ever get that good. According to the debugging mindset, what kind of skill is debugging?",
    "explanation": "Debugging is a learnable procedure: form a clear idea of what the code should do, observe what it actually does, and systematically narrow the gap between the two, rather than an innate talent some people simply have.",
    "options": [
      "Something that can only be done by rereading code silently",
      "A process that only senior developers can perform well",
      "A learnable process for comparing expected and actual behavior",
      "A fixed personality trait that some people are born with"
    ],
    "answer": "C"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "A new intern on a shopping cart project watches the team lead sprinkle temporary output statements through the code while chasing a bug, and asks what this technique is. What is print debugging?",
    "explanation": "Print debugging means placing deliberate, temporary `print()` calls around suspicious lines to observe a value's actual state at each step, narrowing a vague bug into a precise, specific mismatch between expected and actual values.",
    "options": [
      "Printing only the final output of a program at the very end",
      "A Python function that locates and fixes bugs automatically",
      "Printing the entire source code of a program out to a separate file",
      "Temporary print calls that reveal state during execution"
    ],
    "answer": "D"
  },
  {
    "set": 2,
    "difficulty": "hard",
    "bloom": "analyze",
    "subtopic": "debugging-techniques",
    "description": "While fixing a bug in a to-do list app, a developer inserts temporary print statements around suspicious lines. The bug turns out to be buried deep inside nested function calls that run hundreds of times per second, flooding the terminal. What does this situation show about print debugging?",
    "explanation": "Print debugging works well for a specific, localized suspicion but does not scale well to deeply nested logic, since rerunning and re-reading scattered prints across multiple files is slow and easy to lose track of; an interactive debugger is better suited here.",
    "options": [
      "Print debugging scales best because it shows every iteration",
      "Print debugging is always the best tool regardless of bug depth",
      "It scales poorly; pdb can inspect the nested state directly",
      "The bug cannot be found without rewriting the entire program"
    ],
    "answer": "C"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "analyze",
    "subtopic": "debugging-techniques",
    "description": "A developer's program crashes, and they immediately start changing random lines and rerunning, hoping something works. What is the main problem with this approach?",
    "explanation": "Randomly changing code without forming a specific theory about the cause is the most common unproductive response to an error, and it usually takes longer than calmly reading the traceback and testing one clear hypothesis at a time.",
    "options": [
      "It is the fastest way to fix any bug",
      "Python refuses to run after three failed attempts",
      "It is inefficient only when the program has many files",
      "It tests no specific theory, so changes become guesswork"
    ],
    "answer": "D"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "A team-splitting helper is under review.\n```python\ndef split_into_teams(attendees, team_size):\n    return len(attendees) // team_size\n```\nWhy is predicting that `team_size=0` will break this function, before ever running it, a sign of good debugging mindset?",
    "explanation": "Anticipating which inputs would break a function, here a `team_size` of 0 causing a `ZeroDivisionError`, shows the habit of asking 'what input would break this' proactively, which is exactly the calm, structured questioning this mindset is meant to build.",
    "options": [
      "It proactively tests an assumption with a likely failure case",
      "It proves the function is unusable for every possible input",
      "It means the entire function should simply be deleted",
      "It has no real value since Python catches all such errors automatically"
    ],
    "answer": "A"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "A parser may receive text with hidden whitespace.\n```python\nvalue = \"3 \"\nprint(f\"Value: {value}\")\nprint(f\"Value: {value!r}\")\n```\nWhy does the second print line catch bugs the first one would miss?",
    "explanation": "Using !r prints the value's `repr()`, the precise unambiguous form, which reveals subtle differences like a trailing space, an empty string versus `None`, or '3' versus 3 that a plain f-string display would render identically and hide.",
    "options": [
      "`!r` makes the program execute faster",
      "`repr()` exposes details such as the trailing space",
      "!r converts the value into an integer before it is ever printed",
      "!r is required syntax and has no effect on the displayed text"
    ],
    "answer": "B"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "Why should debug `print()` statements added temporarily during investigation be removed or commented out once the bug is fixed?",
    "explanation": "Leftover debug prints clutter the real program output and confuse anyone reading it later, including your future self, so once they have served their purpose of finding the bug they should be removed or clearly commented out.",
    "options": [
      "They clutter real output and confuse future readers",
      "Python automatically deletes unused print statements",
      "They make the program run out of memory over time",
      "Leaving them in always causes a `SyntaxError`"
    ],
    "answer": "A"
  },
  {
    "set": 2,
    "difficulty": "hard",
    "bloom": "analyze",
    "subtopic": "assertions",
    "description": "A developer uses assert to check that a bank withdrawal never overdraws the account, planning to rely on that check even in production. Why is assert risky for a check that must always run, and what should be used instead?",
    "explanation": "Python can run with assertions stripped out entirely using the -O optimization flag, so any check that must always run for correctness, like rejecting an overdraft, needs a real if/raise rather than relying on assert, which is only a development-time sanity check.",
    "options": [
      "assert cannot compare numbers, only strings",
      "There is no practical difference between `assert` and `if`/`raise`",
      "Assertions may be disabled with `-O`; use `if`/`raise`",
      "if/raise is slower and only used for style reasons"
    ],
    "answer": "C"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "analyze",
    "subtopic": "debugging-techniques",
    "description": "A developer suspects a bug occurs only when a specific attendee's `rsvp_count` is negative, inside a loop processing many attendees. What is the most effective use of `breakpoint()` here?",
    "explanation": "Placing `breakpoint()` inside the loop body lets the developer pause on every iteration and inspect each attendee's `rsvp_count` directly, or combine it with a conditional check so the pause only triggers when the suspicious condition is actually met, rather than guessing from outside the loop.",
    "options": [
      "`breakpoint()` can only ever be placed before the loop begins, never inside it at all",
      "Call `breakpoint()` once before the loop and let it detect negatives automatically",
      "There is no way at all to inspect any loop variables using pdb here",
      "Place `breakpoint()` inside `if attendee.rsvp_count < 0` in the loop"
    ],
    "answer": "D"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "apply",
    "subtopic": "assertions",
    "description": "An event summary function is tested with no attendees.\n```python\ndef average_rsvps(attendees):\n    assert len(attendees) > 0, \"attendees list must not be empty\"\n    total = sum(a.rsvp_count for a in attendees)\n    return total / len(attendees)\n\naverage_rsvps([])\n```\nWhat happens when this runs?",
    "explanation": "Since the attendees list is empty, `len(attendees)` > 0 is `False`, so Python immediately raises `AssertionError`: attendees list must not be empty at that line, rather than letting execution continue to a less informative `ZeroDivisionError` two lines later.",
    "options": [
      "The assert immediately raises `AssertionError` with its message",
      "It returns the value 0 silently with no error at all",
      "It returns `None` because the assertion replaces the return statement",
      "It raises `ZeroDivisionError` on the total / `len(attendees)` line"
    ],
    "answer": "A"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "assertions",
    "description": "An Event class encodes a capacity rule.\n```python\nclass Event:\n    def __init__(self, name, max_attendees):\n        assert max_attendees > 0, \"an event must allow at least one attendee\"\n        self.name = name\n        self.max_attendees = max_attendees\n```\nBeyond catching bugs, what additional value does this assert provide?",
    "explanation": "The assert statement makes an assumption about the class explicit and visible to anyone reading the code, documenting directly in the code that a valid Event can never have zero or negative capacity, rather than leaving that rule only in the original author's head.",
    "options": [
      "It replaces the need for the constructor",
      "It documents the class invariant beside the code",
      "It makes the entire class run measurably faster",
      "It allows `max_attendees` to be any negative number safely"
    ],
    "answer": "B"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "A developer evaluates a condition while execution is paused.\n```\n(Pdb) p attendee.rsvp_count > 5\n```\n`False`\nWhat does the `p` command do in this pdb session?",
    "explanation": "The p command prints the value of any expression, not just a bare variable name, which is useful for checking something more specific, like a comparison or a computed value, directly at the paused point in execution.",
    "options": [
      "It permanently changes the value used in the expression",
      "It plots an entire graph of the variable's history",
      "It pauses the entire program permanently, forever",
      "It evaluates and prints an expression in the paused frame"
    ],
    "answer": "D"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "A debugger pauses just before a nested function call.\n```python\ndef summarize_event(attendees):\n    breakpoint()\n    total = total_rsvps(attendees)\n    return f\"Total RSVPs: {total}\"\n```\nPaused at the `total_rsvps(attendees)` line, what is the key difference between typing n and typing s here?",
    "explanation": "n runs the entire `total_rsvps` call as one step and pauses on the line after it without ever showing what happened inside, while s steps into `total_rsvps` itself, pausing at its very first line so you can follow execution downward into that function.",
    "options": [
      "n steps into the function, while s skips over it entirely",
      "`n` steps over the call; `s` steps into it",
      "s quits the debugger entirely, while n simply continues it",
      "Both commands run the call and pause on the following line"
    ],
    "answer": "B"
  },
  {
    "set": 2,
    "difficulty": "easy",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "While chasing a scoring bug in a game leaderboard script, a developer pauses execution in pdb and now wants to move forward one line at a time versus letting the rest of the program run freely. Once paused in pdb, what is the difference between typing n and typing c?",
    "explanation": "n (next) runs only the current line and pauses again on the following one, letting you step through code one line at a time, while c (continue) resumes normal execution until the next `breakpoint()` call or the program's end.",
    "options": [
      "`n` quits the debugger, while `c` advances one line",
      "n prints all variables, while c prints only the current one",
      "`n` advances one line; `c` resumes to the next stop",
      "n and c do exactly the same identical thing every time"
    ],
    "answer": "C"
  },
  {
    "set": 2,
    "difficulty": "easy",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "After stepping through several lines in pdb and losing track of the surrounding code, which command shows the source code around the current paused line with an arrow marking the exact spot?",
    "explanation": "The l (list) command prints the source code surrounding the current paused line, with an arrow marking exactly where execution stopped, which is useful for regaining context after several steps.",
    "options": [
      "l",
      "w",
      "s",
      "n"
    ],
    "answer": "A"
  },
  {
    "set": 2,
    "difficulty": "hard",
    "bloom": "analyze",
    "subtopic": "debugging-techniques",
    "description": "A developer steps into a function with s, uses u to check that a variable looked correct one level up in the caller, then uses d to come back down. The variable looks wrong only after the inner function runs. What does this narrow down?",
    "explanation": "Confirming the variable was correct in the caller before the inner function touched it, but wrong afterward, narrows the bug specifically to logic inside the inner function itself, rather than to bad data arriving from outside it.",
    "options": [
      "The bug is definitely caused by Python's garbage collector",
      "The inner function becomes the strongest suspect",
      "The bug must be in the caller instead, not the inner function",
      "The observations provide no useful clue about the bug's location"
    ],
    "answer": "B"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "apply",
    "subtopic": "debugging-techniques",
    "description": "A developer asks pdb to show how execution reached the current line.\n```\n(Pdb) w\n  tracker.py(12)<module>()\n-> summarize_event(attendees)\n  tracker.py(8)summarize_event()\n-> total = total_rsvps(attendees)\n> tracker.py(3)total_rsvps()\n-> total += attendee.rsvp_count\n```\nReading this output bottom to top, what does it represent?",
    "explanation": "The w (where) command shows the full call stack, and reading from bottom to top shows the current paused location first, then each function that is waiting for it to finish, one level further out, all the way up to the outermost call.",
    "options": [
      "The current frame and the chain of callers above it",
      "The order in which variables were created over time",
      "The order in which functions are expected to run in the future",
      "A list of all functions defined anywhere in the file"
    ],
    "answer": "A"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "apply",
    "subtopic": "debugging-techniques",
    "description": "Paused deep inside `total_rsvps`, a developer wants to check the value of attendees back in `summarize_event` without resuming execution. Which commands accomplish this?",
    "explanation": "u (up) moves the inspection point one level up the call stack to the caller, `summarize_event`, allowing inspection of its local variables like attendees without resuming, and d (down) moves back down toward the current paused line afterward.",
    "options": [
      "s and w",
      "u and d",
      "n and c",
      "p and pp"
    ],
    "answer": "B"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "debugging-techniques",
    "description": "Why is the call stack, viewable with w, especially useful for a bug buried three or more function calls deep?",
    "explanation": "When a bug is buried several calls deep, it becomes easy to lose track of who called what; w shows the entire chain of waiting callers at once, letting a developer see the full path execution took to reach the current paused point instead of guessing it.",
    "options": [
      "It is only useful for single-function programs",
      "It replaces the need to pause execution with a breakpoint",
      "It shows the caller chain that led to the current frame",
      "It automatically fixes any bug found in the call chain"
    ],
    "answer": "C"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "apply",
    "subtopic": "logging",
    "description": "A service configures logging at `WARNING`.\n```python\nimport logging\nlogging.basicConfig(level=logging.WARNING)\nlogging.debug(\"detail\")\nlogging.info(\"status\")\nlogging.warning(\"caution\")\nlogging.error(\"failure\")\n```\nWhich messages are actually shown?",
    "explanation": "Setting the level to WARNING shows only messages at WARNING severity or higher, so both the debug and info messages are silently skipped while the warning and error messages are shown.",
    "options": [
      "No messages are shown at all",
      "All four messages are shown",
      "Only the warning and error messages",
      "Only the debug and info messages are shown"
    ],
    "answer": "C"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "apply",
    "subtopic": "logging",
    "description": "A service fails to open one report file, records the failed operation, and continues serving other requests. Which logging level is most appropriate?",
    "explanation": "`logging.error()` (or `logging.critical()` for an unrecoverable, program-ending failure) is appropriate for something that has genuinely failed, as opposed to debug for fine detail, info for routine status, or warning for a recoverable but noteworthy issue.",
    "options": [
      "`logging.warning()`",
      "`logging.error()`",
      "`logging.debug()`",
      "`logging.info()`"
    ],
    "answer": "B"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "logging",
    "description": "A developer maintaining a weather-alert app wants to track routine status updates and unusual conditions without cluttering the console the way scattered `print()` calls do. What is the main advantage of `logging.info()` and `logging.warning()` over plain `print()` statements for tracking a program's behavior?",
    "explanation": "logging labels each message with its severity automatically, can be silenced by level without touching the code that produces the messages, and can redirect output to a file via `basicConfig(filename=...)`, none of which plain `print()` offers without writing that logic yourself.",
    "options": [
      "Severity labels, level filters, and optional file output",
      "`print()` cannot accept f-strings while logging can",
      "`print()` and logging provide the same filtering and severity features",
      "logging is only available in Python versions after 3.10"
    ],
    "answer": "A"
  },
  {
    "set": 2,
    "difficulty": "easy",
    "bloom": "understand",
    "subtopic": "debugging-workflow",
    "description": "A bug in a large inventory-management system only shows up after processing thousands of records, making it painful to trace through the full program every time. What is the purpose of isolating a minimal failing case when debugging?",
    "explanation": "Isolating a minimal failing case means stripping away everything not required to trigger a bug, one piece at a time, until a small, self-contained reproduction remains, turning a vague 'something is wrong somewhere' into a specific, answerable question.",
    "options": [
      "To make the bug much harder to find on purpose here",
      "Keep removing unrelated code while preserving the failure",
      "To rewrite the entire program completely from scratch every time",
      "To hide the failure from anyone reviewing the code"
    ],
    "answer": "B"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "understand",
    "subtopic": "debugging-workflow",
    "description": "Stuck on a stubborn bug in a chat app's message parser, a developer starts explaining the code out loud line by line to a rubber duck on the desk, and the mistake suddenly becomes obvious. Why does rubber-duck debugging, explaining code out loud sentence by sentence, often surface bugs that silent reading misses?",
    "explanation": "Reading code silently lets your eyes skip over a line you already believe is correct, relying on assumptions you already hold, while explaining the same code out loud forces you to state each assumption explicitly, and a wrong assumption sounds noticeably wrong the moment it is spoken.",
    "options": [
      "The physical rubber duck provides hints through careful listening",
      "The duck provides hints based on machine learning models",
      "Speaking forces hidden assumptions to become explicit",
      "It works by literally compiling the spoken words into code"
    ],
    "answer": "C"
  },
  {
    "set": 2,
    "difficulty": "hard",
    "bloom": "analyze",
    "subtopic": "debugging-workflow",
    "description": "A developer has reduced a bug to a function and a class that wraps a single list, and removing the class still reproduces the exact same error message. Should they stop here?",
    "explanation": "No, the developer should keep removing pieces one at a time and rerunning after each removal; since the class was already shown to be unnecessary, the wrapping class should be dropped and the search should continue with the plain list alone until nothing more can be removed without losing the failure.",
    "options": [
      "Yes, any reproduction that crashes is automatically minimal",
      "No. Remove the irrelevant class and keep minimizing",
      "Yes, because removing more code would change the error message",
      "No, minimization should restart from the entire original project"
    ],
    "answer": "B"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "apply",
    "subtopic": "debugging-workflow",
    "description": "A reduced test case removes every class from the original program.\n```python\ncounts = [2, \"3\"]\nprint(sum(counts))\n```\nThis crashes identically to a version using an Attendee class. What does that confirm?",
    "explanation": "Since the bug reproduces identically without the Attendee class, the class itself is irrelevant to the bug; the problem is purely about a string ending up inside a list of numbers, narrowing the search away from the class entirely.",
    "options": [
      "The crash still proves that classes caused the bug",
      "The Attendee class is definitely the cause of the bug",
      "`sum()` cannot ever be used on any list containing numbers",
      "The bug is mixed numeric and string data, not the class"
    ],
    "answer": "D"
  },
  {
    "set": 2,
    "difficulty": "medium",
    "bloom": "apply",
    "subtopic": "assertions",
    "description": "A requirement says inventory must never become negative. Which code most directly turns that invariant into an executable debugging check?",
    "explanation": "An assertion documents and checks the invariant at the point where a negative value would reveal a defect.",
    "options": [
      "Add `assert inventory >= 0` after the update",
      "Print `inventory` without checking whether the requirement holds",
      "Convert inventory to a string, which prevents a numeric invariant from being tested",
      "Catch every exception and replace inventory with zero, hiding the operation that caused the defect"
    ],
    "answer": "A"
  },
  {
    "set": 2,
    "difficulty": "hard",
    "bloom": "analyze",
    "subtopic": "debugging-techniques",
    "description": "Compare adding many temporary `print()` calls with using a breakpoint to inspect a changing state inside a loop. Which approach is better for examining several variables at one exact iteration?",
    "explanation": "A breakpoint pauses at the relevant iteration and supports inspection without scattering temporary output through the code.",
    "options": [
      "Use a conditional breakpoint at the target iteration",
      "Add unrelated print calls in every function, producing more output than the defect requires",
      "Change variable names until the bug disappears",
      "Use neither approach and infer the changing state only from the final screen"
    ],
    "answer": "A"
  },
  {
    "set": 2,
    "difficulty": "hard",
    "bloom": "analyze",
    "subtopic": "debugging-workflow",
    "description": "A quantity validator should accept every integer from zero upward, but the original code used `quantity > 0`. A teammate changes it to `quantity >= 0` and reports success after testing only `10`. Which verification gives the strongest evidence that the boundary bug is fixed without masking a nearby regression?",
    "explanation": "The original failing boundary, zero, must pass after the fix. Testing one value on each side as well, 1 and -1, confirms the corrected comparison accepts nearby valid input and still rejects nearby invalid input instead of merely looking plausible.",
    "options": [
      "Test only `10` again because it is a typical valid value",
      "Read the changed operator without executing the function",
      "Replace the validation with a broad exception handler",
      "Run `0`, `1`, and `-1` to verify both sides of the boundary"
    ],
    "answer": "D"
  }
]
